import 'package:flutter/material.dart';

class LotusLoader extends StatelessWidget {
  const LotusLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(
        Icons.local_florist,
        color: Colors.amber,
        size: 40,
      ),
    );
  }
}